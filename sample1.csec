def add(a: Int, b: Int): Int {
    var sec = if (a > 100) {
        a;
    }
    else {
        b;
    };

    return a + b + sec;
}

def sample1(a: Int, b: Int) = a + b;

def sample2(): String {
    return "Hello";
}

def sample3(): Int {
    var s = 10;
    for (i <= 0 to 10) {
        s += 1;
    }
    return s;
}

def sample4() = $$\sum_{i=0}^{10} i$$

def main(): Int {
    var result: Int = add(10, 20);
    return result;
}