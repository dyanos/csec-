def main(): Int {
    var total: Int = 0;

    for (i <- 0 to 2) {
        if (i < 2) {
            while (total < 10) {
                total = total + i + 1;
            }
        }
        else {
            total = total + 100;
        }
    }

    return total;
}
