def main(): Int {
    val xs = [1, 2, 3, 4, 5];

    /* sum */
    reduce (x <- xs, 0) {
        x + 1;
    }

    /* product-like */
    reduce (y <- xs, 1) {
        y * 2;
    }

    /* with starting value */
    reduce (z <- xs, 100) {
        z + 1;
    }

    return 0;
}
