def main(): Int {
    val inc = [&](x) -> {
        return x + 1;
    };

    val double = [&](x) -> {
        return x + x;
    };

    val negate = [&](x) -> {
        return 0 - x;
    };

    return 0;
}
