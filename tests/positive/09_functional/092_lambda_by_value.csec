def main(): Int {
    val square = [=](x) -> {
        return x * x;
    };

    val add = [=](a, b) -> {
        return a + b;
    };

    val isPositive = [=](x) -> {
        if (x > 0) {
            return 1;
        }
        else {
            return 0;
        }
    };

    return 0;
}
