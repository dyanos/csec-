def identity(values: Vector[Char]): Vector[Char] = values;

def main(): Int {
    val values = new Char[2];
    values[0] = 'C';
    values[1] = 'D';
    val forwarded: Vector[Char] = identity(<- values);
    if (forwarded[1] == 'D') {
        return 0;
    }
    return 1;
}
