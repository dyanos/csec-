def build(size: Int): Array[Int] {
    val values = new Int[size];
    values[0] = 40;
    values[1] = 41;
    values[2] = 42;
    return values;
}

def main(): Int {
    val made: Array[Int] = build(3);
    if (made[2] == 42) {
        return 0;
    }
    return 1;
}
