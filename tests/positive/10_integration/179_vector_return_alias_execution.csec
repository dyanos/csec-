def build(size: Int): Vector[Int] {
    val values = new Int[size];
    values[0] = 40;
    values[1] = 42;
    return values;
}

def main(): Int {
    val made: Vector[Int] = build(3);
    if (made[1] == 42) {
        return 0;
    }
    return 1;
}
