def main(): Int {
    val a: Tensor<Int, 2, 2> = new Tensor<Int, 2, 2>(1);
    val b: Tensor<Int, 2, 2> = new Tensor<Int, 2, 2>(2);

    val sum = a + b;
    val diff = a - b;
    val product = a * b;
    val scaled = a * 3;
    val matmul = a @ b;

    return 0;
}
